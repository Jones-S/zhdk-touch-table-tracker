directories:
  output: ./output

mac:
  # category: public.app-category.productivity
  # icon: ./build-helpers/images/icon.icns
  target:
    target: dmg
    arch:
      - x64
      - arm64

win:
  target:
    target: nsis
    arch:
      - x64
      - ia32
